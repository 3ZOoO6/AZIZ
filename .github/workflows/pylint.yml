الاسم: GitHub Actions Demo
run-name: ${{ github.actor }} يختبر GitHub Actions 🚀
على: [دفع]
الوظائف:
  استكشاف-GitHub-Actions:
    يعمل على: ubuntu-latest
    خطوات:
      - تشغيل: echo "🎉 تم تشغيل المهمة تلقائيًا بواسطة حدث ${{ github.event_name }}."
      - تشغيل: echo "🐧 يتم تشغيل هذه المهمة الآن على خادم ${{ runner.os }} المستضاف بواسطة GitHub!"
      - تشغيل: echo "🔎 اسم فرعك هو ${{ github.ref }} ومستودعك هو ${{ github.repository }}."
      - الاسم: التحقق من رمز المستودع
        الاستخدامات: actions/checkout@v4
      - تشغيل: echo "💡 تم استنساخ مستودع ${{ github.repository }} إلى المشغل."
      - تشغيل: echo "🖥️ أصبح سير العمل جاهزًا الآن لاختبار الكود الخاص بك على المشغل."
      - الاسم: قائمة الملفات الموجودة في المستودع
        تشغيل: |
          ls ${{ github.workspace }}
      - تشغيل: echo "🍏 حالة هذه الوظيفة هي ${{ job.status }}."
